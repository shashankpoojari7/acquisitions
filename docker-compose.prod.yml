version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: acquisitions-app
    env_file:
      - .env.production
    environment:
      NODE_ENV: "production"
    ports:
      - "3000:3000"
    command: ["npm", "run", "start"]