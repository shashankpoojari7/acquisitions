name: Tests

on:
  push:
    branches: ['main', 'staging']
  pull_request:
    branches: ['main', 'staging']

jobs:
  test:
    name: Run Jest tests
    runs-on: ubuntu-latest
    env:
      NODE_ENV: test
      NODE_OPTIONS: --experimental-vm-modules
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        id: test
        run: npm test
        continue-on-error: true

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage
          retention-days: 30

      - name: Add test summary
        if: always()
        run: |
          echo "## Test Results" >> "$GITHUB_STEP_SUMMARY"
          if [ "${{ steps.test.outcome }}" = "success" ]; then
            echo "- Status: Success" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- Status: Failed" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [ -d coverage ]; then
            echo "- Coverage directory: ./coverage" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- Coverage: not generated" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Annotate and fail on test failures
        if: steps.test.outcome == 'failure'
        run: |
          echo "::error title=Tests failed::One or more tests failed. Run 'npm test' locally to reproduce and fix the issues before pushing again.";
          exit 1
